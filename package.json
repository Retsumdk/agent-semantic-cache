{
  "name": "agent-semantic-cache",
  "description": "Semantic caching layer for LLM responses to reduce latency and cost in multi-agent systems",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}